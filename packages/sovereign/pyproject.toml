[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sovereign-orchestrator"
version = "0.1.0"
description = "Sovereign orchestrator core: agents, queue, scheduler, memory, tools, governance, handshake, HTTP API"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["sovereign*"]
