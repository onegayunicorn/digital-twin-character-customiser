/**
 * useCustomizerState — the original spec hook name, re-exported from the core
 * state package for drop-in compatibility with the PDF layout.
 */
export { useCustomizerStore as useCustomizerState } from "@dt-core/state";
export type { CustomizerStore } from "@dt-core/state";
